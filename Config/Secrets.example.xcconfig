// Copy to Config/Secrets.xcconfig (gitignored). CI materializes the real file
// from the SECRETS_XCCONFIG GitHub Actions secret.
//
// These values ship inside the app binary regardless. Keeping them out of git
// protects the repository, not the runtime.
SENTRY_DSN =
